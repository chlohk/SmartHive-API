package com.example.SmartHiveAPI.repositories;

import com.example.SmartHiveAPI.model.PlanningDropdown;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlanningDropdownRepository extends JpaRepository<PlanningDropdown, Long> {
}
